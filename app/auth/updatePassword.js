import { StyleSheet, View } from "react-native";

import VibeText from "@/components/VibeText.js";

function UpdatePassword(props) {
    return (
        <View>
            <VibeText weight="SemiBold">
                Update Password
            </VibeText>
        </View>
    );
}
const styles = StyleSheet.create({
    
})
export default UpdatePassword;